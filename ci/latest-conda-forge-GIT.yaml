name: test
channels:
  - conda-forge
dependencies:
  # required
  - esda
  - numpy
  - rtree
  - watermark
  # testing
  - pytest
  - pytest-cov
  - codecov
  # required
  - pip
  - pip:
    - git+https://github.com/pysal/libpysal.git@master
