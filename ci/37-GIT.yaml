name: test
channels:
  - conda-forge
dependencies:
  - python=3.7
  # required
  - esda
  - numpy
  - pandas>=1.0
  - rtree
  - scipy>=1.0
  - watermark
  # testing
  - pytest
  - pytest-cov
  - codecov
  # required
  - pip
  - pip:
    - git+https://github.com/pysal/libpysal.git@master
