name: test
channels:
  - conda-forge
dependencies:
  - python=3.7
  # required
  - libpysal
  - esda
  - numpy
  - pandas>=1.0
  - rtree
  - scipy>=1.0
  - watermark
  # testing
  - pytest
  - pytest-cov
  - codecov
